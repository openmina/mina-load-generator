{
  "name": "jobs-controller",
  "version": "0.0.1",
  "description": "Controller for CI parallel jobs",
  "main": "main.ts",
  "scripts": {
    "build": "tsc --build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx eslint src/* --fix"
  },
  "lint-staged": {
    "src/**/*.ts": [
      "eslint src/* --fix",
      "prettier --write --ignore-unknown"
    ]
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commander-js/extra-typings": "^10.0.2",
    "@types/commander": "^2.12.2",
    "@types/express": "^4.17.17",
    "@types/node": "^18.14.2",
    "typescript": "^4.9.5"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "tslog": "^4.8.2"
  }
}
